# voice-profile 이력 (규칙 추출 근거 — 초안 작성 시 로드 불필요)

## 블라인드 테스트 이력

- 2026-08-24 1차: 5 생성 + 5 실제, 판별 3/5 → **실패.** 걸린 3건의 공통 결함 = 경구형 마무리
  (대구 2건, 시그니처 토큰 복제 1건). 통과 2건도 약한 대구 포함 — 대구는 강도 문제가 아니라 사용 자체가 위험.
  실제 글 2건(오탈자·절단 포함)이 AI로 오인됨 → 판별축은 "매끈함". 규칙 10 교체(마무리 4종 한정)
  + SKILL 자체 검사에 대구 전수 검사·시그니처 토큰 금지 추가로 대응 (12개 상한 유지).

- 2026-08-24 2차 (패치 후): 생성 5 + 실제 2, 7택5 강제선택. 적중 4/5 — 단 설계상 최소 3개 자동 적중이라
  유효 정보는 역전 1건뿐: 실제 글(11, IT은유 밈체)이 AI로 오분류, 생성 1건(절단형)은 통과.
  잔여 결함 진단: ① 대칭 정의 커플릿(대구의 변장) ② 펀치라인 밀도 균질. SKILL 검사 2항 추가.
  통과한 생성물의 공통점 = 짧고 건조하거나 미완결(절단). **잘 쓸수록 걸린다는 축은 2차에서도 유지됨.**

## 변경 이력

- v2 (2026-08-24): 레지스터 3종 허용 → **압축체 단일 고정**. 계정 클러스터 정규화로
  규칙 12개 재검증 (전부 3+ 클러스터 통과, 단 규칙 10 후반부는 2클러스터로 약함).
  단일 계정 버릇 7종 강등·격리. v1 규칙 1(유형별 레지스터 매핑) 폐기.
- v1 (2026-08-24): 최초 추출 (조회수 없는 스와이프 20건).

## v3 — 2026-08-27 (실측 전환)

첫 `npm run sync` 로 조회수 확보. v1·v2 는 스와이프 코퍼스 20건(조회수 없음) 기반이었고,
v3 부터 내 계정 65건 WIN/LOSS 대조가 근거다.

**표본**: 발행 77건 → 링크 답글 1건 제외 → 중복 재게시 11건 제외 → 65건.
파이프라인 시기(08-25~26) 49건에서 WIN 10(중앙 2,922) / LOSS 10(중앙 98).

**규칙 변경 (12개 → 10개)**
- 신설 1위 [분기]: 일간·일지 분기 WIN 6.4줄 / LOSS 0줄. 단일 최대 변수.
- 신설 [훅] 하한: 첫 줄 12자 이하 절단 조각 = LOSS 50% / WIN 10%. lr-022~031 배치 전멸(평균 189).
  v2 규칙 4 [절단]은 "캐러셀 1장 끝"에 한정되도록 축소 — 첫 줄 절단과 혼동해 사고가 났다.
- 승격 [마무리]: 만세력·전체사주 안내 WIN 70% / LOSS 0%. 한정 조항이 면책이 아니라 성과 요인.
- 신설 [길이] 하한 300자: WIN 369 / LOSS 170.
- 삭제: v2 규칙 8 [비유](WIN/LOSS 차이 없음), 규칙 11 일부 흡수.
- 보류 처리: 해시태그·번호리스트·발행시각은 교란 분리 불가 → 규칙화하지 않고 표로만 기록.

**few-shots**: 코퍼스 7건 → 실측 WIN 6건(1부) + 코퍼스 3건(2부).
1위 글(9,063 views)은 자축합 오류를 포함해 few-shots 에서 제외했다 — 성과는 최고지만 모범 원문으로 쓸 수 없다.

**미검증**: 08-26분은 수집 시점 19~27시간이라 미성숙. 다음 sync 후 순위 재확인 필요.
블라인드 테스트는 아직 안 돌림.

## v4 — 2026-08-30 (표본 158건)

`npm run sync` 로 251 → 309건(본문 172) 회수 후 재추출.
중복 재게시 14건 제외 158건, WIN 32 / LOSS 33. WIN 중앙 2,223 / LOSS 중앙 69.
v3 은 65건(WIN10/LOSS10)이었으므로 표본 2.4배.

### 유지 (4)
- 분기 구조 (5.5 / 0.3) — 여전히 최대 변수
- 구체성·숫자 (88% / 33%)
- 길이 (340자 / 276자)
- 압축체 레지스터

### 강화 (2)
- 시의성 40%/10% → **53% / 12%** (4.4배). 세 코호트 전부 같은 방향
- 만세력 마무리 LOSS 0% 가 표본 3배에서도 유지

### 약화 (2)
- 첫줄 12자 이하: LOSS 50%/WIN 10% → **9% / 6%**. "최악의 단일 패턴" 표현 철회.
  그 형식을 이후 안 썼기 때문에 희석된 것이라 방향만 남기고 강도 낮춤
- 한정 조항: 3.0배 → **1.8배**

### 판정 뒤집힘 (2) ★
- **꺾쇠 `<제목>` → 판정 보류로 강등.** 코호트마다 뒤집힌다
  (08-26 229<269 / 08-27 126<132 / 08-29 1174>322). v3 도 단서를 달았으나
  표본 3배에서도 지지되지 않음. **CLAUDE.md 의 "첫 줄 꺾쇠 고정" 규칙은 실측 근거가 없다.**
  규칙 유지 여부는 사람 결정 사항이라 문서에서 제거하지 않고 보류로만 표시.
- **댓글 CTA → 판정 보류로 강등.** v3 의 "유일하게 작동한 CTA" 는 **오판**.
  전체 40/173 으로 오히려 음의 방향. 13건 중 11건이 08-21 존대체 배치에 몰려 있어
  CTA 효과와 시기·문체가 분리되지 않는다.

### 규칙 정의 수정 (1)
- 규칙 1 을 "간지 글자"가 아니라 **"독자가 자기 것을 특정할 수 있는 갈래"**로 재정의.
  근거: 08-29 간지 없는 상위글 2건(2,520 / 1,455)이 신살·구체상황으로 분기했음.
  반대로 십신만 추상 서술한 글(08-21·08-27 다수)은 전부 LOSS.

### 파일 변경
- voice-profile.md: v3 → v4 전면 개정 (규칙 10개 → 9개, 판정보류 4항목 → 6항목)
- few-shots.md: WIN 상위에서 구조 다른 10편 재선별 (원문 그대로)
- banned.md: v4 절 추가 37개 (내 계정 172건 전체 말뭉치 기준 0회)

### 미반영 — 다음 sync 숙제
8/29 lr-129~134, 8/30 lr-135~140 총 12편이 72h 미경과로 표본에 없음.
9/2 이후 sync 하여 170건으로 재확인할 것:
- 첫줄 13~24자 짧은 훅 (lr-135 가 13자)
- 6편 동시 발행 2회 연속의 자기잠식
- 비연애(돈·시의성) 소재 6편의 성과 — 지금까지 연애에 쏠려 있었음
